# Create a fresh key after revoking any key that was pasted into chat or source code.
NVIDIA_API_KEY=

# The checker defaults to this model. Change only after running the full comparison.
NVIDIA_MODEL=meta/llama-3.2-11b-vision-instruct

